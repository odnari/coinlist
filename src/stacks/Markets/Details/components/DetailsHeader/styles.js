import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  logo: {
    aspectRatio: 1.0,
    height: 96,
    alignSelf: 'center',
    marginVertical: 16,
  },
});

export default styles;
